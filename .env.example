# Copy this file to .env, then replace the placeholder with the shared VM
# sudo password. The .env file is ignored by Git and should be mode 600.
SEEDSCAN_SUDO_PASSWORD='replace-with-local-password'
